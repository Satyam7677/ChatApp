const defaultPath = '../../assets/images/'
const images = {
    chatIcon:require(`${defaultPath}chat.png`),
    whiteImage:require(`${defaultPath}whiteImage.png`),
    options:require(`${defaultPath}options.png`),
    camera:require(`${defaultPath}camera.png`),
    user:require(`${defaultPath}user.png`) ,
    info:require(`${defaultPath}info.png`),
    call:require(`${defaultPath}call.png`),
    videoCall:require(`${defaultPath}camcorder.png`),
    back:require(`${defaultPath}arrow.png`),
    splashImage:require(`${defaultPath}slashImage.jpeg`),
}
export default images